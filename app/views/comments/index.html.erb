<!-- need to fix -->
<h2><%=link_to(@comment.content, post_comment_path(@comment))%></h2>

<span><%= link_to 'edit', edit_post_comment_path(@comment) %></span>
<span><%= link_to 'delete', post_comment_path(@comment), :method => :delete %></span>

<%= link_to 'Create new comment', new_post_comment_path %>
<ul>
  <% @comments.each do |comment| %>
    <li><%= comment.content %></li>
  <% end %>
</ul>
